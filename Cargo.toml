[package]
name = "lenscast"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { version = "0.11", package = "gtk4" }
gdk = { version = "0.11", package = "gdk4" }
adw = { version = "0.9", package = "libadwaita" }
glib = "0.21"

[profile.release]
strip = true